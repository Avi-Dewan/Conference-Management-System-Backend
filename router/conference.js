const express = require('express');
const db = require('../db/database'); 

const router = express.Router();

// conference_id, conference_title, conference_description, conference_webpage, start_date, end_date, submission_deadline(date and time), related_fields (array)


module.exports = router;

